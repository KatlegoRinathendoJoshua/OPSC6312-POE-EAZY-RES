package com.eazyres.app

import android.app.Application

/**
 * Application class. Kept minimal — used as an entry point for any
 * future global initialisation (crash reporting, notification channels, etc).
 */
class EazyResApp : Application()
